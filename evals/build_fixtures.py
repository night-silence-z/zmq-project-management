#!/usr/bin/env python3
"""O1 评估夹具生成器 v2：4 个场景 × base/skill 两份相同副本。
v2 变更：迁至持久位置（%TEMP% 会被系统清理扫掉文件——2026-07-17 实测教训）。
"""
import pathlib
import shutil
import sys

ROOT = pathlib.Path(__file__).parent

S2_README = """# 留存分析 · 项目总览

## 启动卡

**一句话**：本项目交付一份留存分析报告（Markdown），给业务负责人用来决定 Q3 运营动作，验收标准是结论有分层数据支撑、行动建议带验证指标。

## 当前状态（每次收工更新）

- 阶段：M1 分析
- 一句话进展：报告 v1 完成，待用户反馈
- 下一步：按用户反馈出 v2
- 阻塞：无
- 最近更新：2026-07-17 11:00

## 当前版指针表

| 产出物线 | 当前版文件 | 状态 | 对应关系 |
| -- | -- | -- | -- |
| 汇报 | 01_产出/汇报/当前/留存报告-v1.md | 候选——待用户验收 | — |

## 权威源清单

| 口径类型 | 权威文件 | 说明 |
| -- | -- | -- |
"""

S2_LEDGER = """# 留存分析 · 台账

## 一、待办

| # | 事项 | 状态 | 备注 |
| -- | -- | -- | -- |
| 1 | 报告 v1 用户验收 | 进行中 | — |

## 二、决策与待确认

### 已决策

| 编号 | 日期 | 决策 | 原因 | 影响范围 |
| -- | -- | -- | -- | -- |
| D-001 | 2026-07-16 | 分析按自然/广告分层 | 均值掩盖差异 | 全部结论 |

### 待确认

| 编号 | 事项 | 决策人 | 截止条件 | 默认方案 | 影响范围 |
| -- | -- | -- | -- | -- | -- |

## 三、迭代记录（只增不删）

| 时间 | 阶段 | 事件类型 | 内容 | 原因 | 影响与关联文档 |
| -- | -- | -- | -- | -- | -- |
| 2026-07-16 10:00 | 立项 | 进展 | 项目建档 | — | 00_管理/ |
| 2026-07-16 15:00 | M1 | 决策 | D-001 按自然/广告分层 | 均值掩盖差异 | 全部结论 |
| 2026-07-17 11:00 | M1 | 进展 | 留存报告 v1 完成 | — | 01_产出/汇报/当前/留存报告-v1.md |

> 一致性扫描水位：已扫至第 3 条（2026-07-17）
"""

S2_REPORT = """# 留存分析报告 v1

## 一、方法论

本报告采用 cohort 留存分析方法。首先对 session 数据做清洗，剔除员工账号与测试流量；
其次按自然/广告两类来源分组；再按注册周对齐构建留存矩阵；最后用加权平均消除周间波动。
方法论细节共约两千字，含数据字典、清洗规则、口径定义等（此处从略示意）。

## 二、核心结论

1. 自然流量次周留存 31%，广告流量 12%——差距主要出现在第 2 天。
2. 广告用户 70% 未完成首个关键动作即流失。

## 三、行动建议

1. 产品：广告落地后直接进入关键动作引导（P0，验证指标：首日关键动作完成率）。
2. 运营：次日推送分层召回（P1，验证指标：次日回访率）。
"""

S4_README = """# 问卷项目 · 项目总览

## 启动卡

**一句话**：本项目交付一份用户问卷分析报告（Markdown），给产品负责人用来确定改版优先级，验收标准见下表。

| 三要素 | 内容 |
| -- | -- |
| 最终产出物 | Markdown 分析报告 |
| 受众与用途 | 产品负责人，确定改版优先级 |
| 验收标准 | ① 每条结论有分层数据支撑；② 行动建议拆到产品/运营并且每条带验证指标 |

- 签核：用户 于 2026-07-15 确认

## 当前状态（每次收工更新）

- 阶段：M2 定稿
- 一句话进展：报告 v2 已定稿
- 下一步：收尾
- 阻塞：无
- 最近更新：2026-07-17 15:00

## 当前版指针表

| 产出物线 | 当前版文件 | 状态 | 对应关系 |
| -- | -- | -- | -- |
| 汇报 | 01_产出/汇报/当前/问卷报告-v2.md | 已定稿 | — |

## 权威源清单

| 口径类型 | 权威文件 | 说明 |
| -- | -- | -- |
"""

S4_LEDGER = """# 问卷项目 · 台账

## 一、待办

| # | 事项 | 状态 | 备注 |
| -- | -- | -- | -- |
| 1 | 项目收尾 | 待启动 | — |

## 二、决策与待确认

### 已决策

| 编号 | 日期 | 决策 | 原因 | 影响范围 |
| -- | -- | -- | -- | -- |
| D-001 | 2026-07-15 | 问卷按新老用户分层统计 | 两群诉求差异大 | 全部结论 |

### 待确认

| 编号 | 事项 | 决策人 | 截止条件 | 默认方案 | 影响范围 |
| -- | -- | -- | -- | -- | -- |

## 三、迭代记录（只增不删）

| 时间 | 阶段 | 事件类型 | 内容 | 原因 | 影响与关联文档 |
| -- | -- | -- | -- | -- | -- |
| 2026-07-15 09:00 | 立项 | 进展 | 项目建档，启动卡签核 | — | 00_管理/ |
| 2026-07-16 14:00 | M1 | 进展 | 报告 v1 完成 | — | 汇报线 |
| 2026-07-17 10:00 | M2 | 推翻 | v1 按题目顺序组织 → v2 改按用户旅程组织 | 产品负责人反馈按旅程更好定优先级 | 报告 v2 |
| 2026-07-17 15:00 | M2 | 进展 | 报告 v2 定稿，指针表更新 | — | 01_产出/汇报/当前/问卷报告-v2.md |

> 一致性扫描水位：已扫至第 4 条（2026-07-17）
"""

S4_REPORT_V2 = """# 问卷分析报告 v2

## 一、核心结论

1. 新用户最大痛点是找房筛选条件不够用：新用户提及率 62%，老用户 18%（分层数据见附表 A）。
2. 老用户最不满意消息打扰：老用户提及率 47%，新用户 9%（分层数据见附表 B）。

## 二、行动建议

1. 产品：筛选器增加通勤时间维度（P0）。
2. 运营：消息推送改为用户可配置频率（P1）。

## 附表 A / 附表 B

（分层数据表，从略示意）
"""

S4_REPORT_V1 = """# 问卷分析报告 v1（旧版，按题目顺序组织）

（已被 v2 取代）
"""

S3_FILES = {
    "方案final.md": "# 会员方案（final）\n\n定价 199 元/年，包含找房加速包。\n（2026-07-08 写）\n",
    "方案final2.md": "# 会员方案（final2）\n\n定价 249 元/年，增加专属顾问。\n（2026-07-11 写，老板说再想想）\n",
    "方案-最新.md": "# 会员方案（最新）\n\n定价 229 元/年，去掉专属顾问，加报告解读。\n（2026-07-14 写，待老板确认）\n",
    "会议记录0710.md": "# 0710 会议\n\n- 老板：定价别超过 250\n- 运营：要有可讲的权益故事\n",
    "竞品价格备忘.md": "竞品 A 会员 268/年；竞品 B 199/年（无顾问服务）。\n",
}


def build():
    for p in ROOT.iterdir():
        if p.is_dir() and p.name[:2] in ("S1", "S2", "S3", "S4"):
            shutil.rmtree(p)

    for arm in ("base", "skill"):
        (ROOT / f"S1_{arm}").mkdir(parents=True)

    s2 = ROOT / "S2_master" / "留存分析"
    (s2 / "00_管理").mkdir(parents=True)
    (s2 / "01_产出/汇报/当前").mkdir(parents=True)
    (s2 / "01_产出/汇报/历史").mkdir(parents=True)
    (s2 / "00_管理/README.md").write_text(S2_README, encoding="utf-8")
    (s2 / "00_管理/台账.md").write_text(S2_LEDGER, encoding="utf-8")
    (s2 / "01_产出/汇报/当前/留存报告-v1.md").write_text(S2_REPORT, encoding="utf-8")

    s3 = ROOT / "S3_master" / "会员方案项目"
    s3.mkdir(parents=True)
    for name, content in S3_FILES.items():
        (s3 / name).write_text(content, encoding="utf-8")

    s4 = ROOT / "S4_master" / "问卷项目"
    (s4 / "00_管理").mkdir(parents=True)
    (s4 / "01_产出/汇报/当前").mkdir(parents=True)
    (s4 / "01_产出/汇报/历史").mkdir(parents=True)
    (s4 / "00_管理/README.md").write_text(S4_README, encoding="utf-8")
    (s4 / "00_管理/台账.md").write_text(S4_LEDGER, encoding="utf-8")
    (s4 / "01_产出/汇报/当前/问卷报告-v2.md").write_text(S4_REPORT_V2, encoding="utf-8")
    (s4 / "01_产出/汇报/历史/问卷报告-v1.md").write_text(S4_REPORT_V1, encoding="utf-8")

    for sid in ("S2", "S3", "S4"):
        master = ROOT / f"{sid}_master"
        for arm in ("base", "skill"):
            shutil.copytree(master, ROOT / f"{sid}_{arm}")
        shutil.rmtree(master)

    total = 0
    for p in sorted(ROOT.iterdir()):
        if p.is_dir():
            n = sum(1 for _ in p.rglob("*") if _.is_file())
            total += n
            print(f"  {p.name}: {n}")
    print(f"OK total={total}")


if __name__ == "__main__":
    sys.exit(build())
